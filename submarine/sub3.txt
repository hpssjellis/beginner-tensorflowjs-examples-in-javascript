TensorFIow.js Iris Classification

Open the browser and type aboutrblank. CTRL+SHIFT+i to open the console. Now execute every block.Note both scripts could be loaded from a html document also.

Ilload tensorflow.js: ”load jquery:

var script = document.createElement('script'); var script = document.createElement('script');
scripttype = text/javascript'; scripttype = “text/javascript';

scriptsrc = 'https'j/cdn.jsdelivr.netlnpm/@ten sorflow/tljs@0.1 1 .1': script.src = 'https://code.jquery.comf1query-3.2.1.min.js':
document.head.appendChiId( script); document.head.appendChiId(script);

load data:
ds = $.getJ SON ("httpsz/lrawgith ubu sercontent.com/domoritz/mapslmaster/datafiris .json");

set up the data:
xs = I];
for(i=0;i<150;i++){ xs.pu sh([[ds.respon seJ SONUH‘sepaILen gth ']],[ds.respon seJ SON[i][‘sepaIV\fidth‘]], [ds.responseJSON[i]['petaILength‘]],[ds.responseJSON[i]['petaIWidth’J]]);}
xs = tf.tensor('xs);

xs = xs.reshape([1 50.41);

3/1 = [I;

for ('i=0;i<50; i++’){y1.push('[1,0,0]')} H Model:

y1 = tf.tensor(y1'); const model = tf.sequential():

y2 = U; model.add(tf.layers.dense(

for (i=0;i<50; i++){y2.push([0,1,0])} {units: 10, activation: ‘sigmoid', inputShape: [xs.shape[1]]}

y2 = tf.tensor(y2‘); model.add(‘tf.layers.dense({ units: 3, activation: 'softmax'}

y3 = fl: const optimizer = tf.train.adam(0.01);

for (i=0;i<50; i++l){ y3.push(,[0.0,1])} model.compile({

y3 = tf.tensor(y3); optimizer: optimizer,

ys = tf.stack([y1.y2ry3]); loss: 'categoricaICrossentropy',

ys = ys.reshape([1 50,31); metrics: ['accu racy’],

D:

it Fit the model:
model.fit(xs, ys, { epochs: 100}

I! Prediction:
pred = tf.tensort[[5.0, 3.4, 1.5, 0.3], [5.3, 22,115,111], shape=[2,4])
model.predict(pred).printo